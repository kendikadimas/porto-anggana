import React from 'react';
import PageRenderer from '../../../components/PageRenderer';
import { pageContent } from '../../../data/pageContent';

const Studio = () => {
    return <PageRenderer content={pageContent['studio']} />;
};

export default Studio;
