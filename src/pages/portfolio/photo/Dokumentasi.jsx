import React from 'react';
import PageRenderer from '../../../components/PageRenderer';
import { pageContent } from '../../../data/pageContent';

const Dokumentasi = () => {
    return <PageRenderer content={pageContent['dokumentasi']} />;
};

export default Dokumentasi;
